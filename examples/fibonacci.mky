let fibonacci = fn(x) {
    if (x == 0 || x == 1) {
        // implicit return statement
        x;
    } else {
        // explicit return statement possible
        return fibonacci(x - 1) + fibonacci(x - 2);
    }
};

print(fibonacci(10));
