print("Welcome to the Monkey Playground!");

let multiplier = fn(factor) {
	return fn(x) {
		x * factor;
	};
};

let double = multiplier(2);
let triple = multiplier(3);

print("The double of 5 is: ", double(5));
print("The triple of 5 is: ", triple(5));
